let arr = [4, "first", "apple", 6, 8, null];
    console.log(arr.length);
for (let i=0; i < arr.length; i++) {
    console.log(arr[i]);
}
//Выведение в консоль количества элементов массива и его каждого элемента.